#include <iostream>
#include <main.h>

using namespace std;

int main() {
    vector<int> p = {1, 5, 8, 9, 10, 17, 17, 20, 24, 30};

//    array<vector<int>, OPTIMAL_VALUE_AND_SOLUTION> res = bottom_cut_rod(p, p.size());
//    vector<int> r = res.at(0);
//    for (auto& e : r) {
//        cout << e << endl;
//    }
//
//    r = cut_rod_aux(p, p.size());
//    for (auto& e : r) {
//        cout << e << endl;
//    }

    string s = "abc";
    cout << s.compare("abc") << endl;

    return 0;
}